{
  "name": "movietrack",
  "version": "0.1.1",
  "private": true,
  "description": "MovieTrack Chrome extension (TypeScript source)",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc -p tsconfig.json",
    "package:webstore": "npm run build && rm -f /tmp/movietrack-webstore.zip && zip -r /tmp/movietrack-webstore.zip manifest.json popup.html popup.css options.html options.css library.html library.css privacy.html icons dist/background.js dist/popup.js dist/options.js dist/library.js dist/config.js dist/types.js && npm run verify:webstore",
    "verify:publish": "npm run typecheck && npm run package:webstore && node scripts/verify-publish-ready.js",
    "verify:webstore": "node scripts/verify-webstore-package.js /tmp/movietrack-webstore.zip",
    "watch": "tsc -w -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.292",
    "typescript": "^5.8.3"
  }
}
