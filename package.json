{
  "name": "movietrack",
  "version": "0.1.0",
  "private": true,
  "description": "MovieTrack Chrome extension (TypeScript source)",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc -p tsconfig.json",
    "package:webstore": "npm run build && rm -f /tmp/movietrack-webstore.zip && zip -r /tmp/movietrack-webstore.zip manifest.json popup.html popup.css privacy.html icons dist/background.js dist/popup.js dist/config.js dist/types.js",
    "watch": "tsc -w -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.292",
    "typescript": "^5.8.3"
  }
}
